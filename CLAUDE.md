@AGENTS.md
# Инструкции для Claude

- Всегда общайся со мной и пиши объяснения исключительно на русском языке.
- Код (переменные, функции, комментарии) пиши в соответствии со стандартами проекта (на английском), но текстовые пояснения в терминале давай на русском.

Правила работы над проектом

- Никогда самостоятельно не делай коммиты (не вызывай `git commit`).
- Никогда не создавай и не переключай ветки (`git checkout`, `git branch`).
- Твоя задача — только изменять код в файлах и тестировать его по моему запросу.
- Все текстовые ответы и объяснения пиши строго на русском языке.
